# Week 6 Design Log

## State:
I am still stuck on dealing with the recursion. I also have an error compiling and simulating my code on asicfab.

## Progress:
- Did some research with the CBG python code to determine if there is a way to do any overlapping between CBG and Benes network to minimize latency.
- Added print statements for the bits produced at each level.
- Figured out that the control bits of first and last stages are produced with the first iteration of recursion (N=32), then second and second-last with N=16, and so on. Therefore, all the control bits (generated by N=2 iteration) must be generated before the 5th Benes stage. The breakdown of the bits produced in CBG and used in Benes network is shown in "crossbar_N32".
- Since control bits generated by N=2 iteration is only needed in the 5th stage, we may be able to overlap the last 4 cycles of CBG to the first 4 cycles of Benes network.
- Considering the workload, we may be able to do the whole permutation in two extra cycles from Benes network (total of 11 cycles, not 15 from the presentation). I will still need to talk to Akshath about what is a maximum load each cycle can have. However, I believe that 6 cycles is long enough to do all the calculations.

- Wrote a systemverilog module for Benes network, including the 2x2 crossbar, output-input logic and pipelining. This will be tested with a hard coded control bits.
- Set up the ssh environment into asicfab
- Got into a problem where my code won't simulate. Getting errors with 'vlog' from Makefile.

- We are now looking into a sorting algorithm, which Duc will be writing a code for.

## Next Steps:
- Fix the error for compiling and simulating
- Run the test and debug
- If it works fine, focus more on flattening the CBG and wait for the result from Duc about sorting algorithm.

## Source code:
- Purdue-SOCET - scratchpad_haejune branch > tensor-core > src > modules > memory > scratchpad > benes_network.sv